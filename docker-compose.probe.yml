services:
  probe:
    build:
      context: .
      dockerfile: Dockerfile.probe
    volumes:
      - ./configs/probe-config.example.json:/config/probe-config.json:ro
      - probe-data:/app/data/probe
    restart: unless-stopped

volumes:
  probe-data:
