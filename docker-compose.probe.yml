services:
  probe:
    build:
      context: .
      dockerfile: Dockerfile.probe
    volumes:
      - ./probe-config.json:/config/probe-config.json:ro
      - ./probe-data:/app/data/probe
    restart: unless-stopped
